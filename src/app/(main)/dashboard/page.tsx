import { NextPage } from "next";
import { ReactElement } from "react";

const Page: NextPage = (): ReactElement => {
  return (
    <div>
      <h1>Dashboard</h1>
    </div>
  );
};

export default Page;
